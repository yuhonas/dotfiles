---
tags: [pacman, packages]
---
# get information about a locally installed package
pacman -Q --info [FILE]

# find which remote packages contains a particular program
# whilst also ensuring the database is up to date
pacman -Fy [FILE]

# find which local package owns a particuar program
pacman -Qo [FILE]

# list files installed by a package
pacman -Ql [FILE]
